<ContentPage 
    x:Class="PIMVIII.Maui.Views.HomePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="#F4F1FF">

    <VerticalStackLayout Padding="25" Spacing="22">

        <Label Text="Painel do Criador"
               FontSize="32"
               FontAttributes="Bold"
               TextColor="#4B42CC"
               HorizontalOptions="Center" />

        <Border StrokeThickness="0"
                BackgroundColor="White">

            <Border.StrokeShape>
                <RoundRectangle CornerRadius="18"/>
            </Border.StrokeShape>

            <Border.Shadow>
                <Shadow Brush="#30000000"
                        Opacity="0.4"
                        Radius="18"/>
            </Border.Shadow>

            <VerticalStackLayout Padding="20" Spacing="14">

                <Button Text="📤 Upload de Conteúdo"
                        Clicked="GoToUpload"
                        HeightRequest="55"
                        BackgroundColor="#6C63FF"
                        CornerRadius="12"
                        TextColor="White"/>

                <Button Text="🎵 Gerenciar Playlists"
                        Clicked="GoToPlaylists"
                        HeightRequest="55"
                        BackgroundColor="#6C63FF"
                        CornerRadius="12"
                        TextColor="White"/>

                <Button Text="📊 Análise de Métricas"
                        Clicked="GoToMetrics"
                        HeightRequest="55"
                        BackgroundColor="#6C63FF"
                        CornerRadius="12"
                        TextColor="White"/>

            </VerticalStackLayout>

        </Border>

    </VerticalStackLayout>

</ContentPage>
